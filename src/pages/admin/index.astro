<script >
    import CMS, { locales } from '@staticcms/core';
    import '@staticcms/core/dist/main.css';
    import { config } from './config';

    // Initialize the CMS object
    CMS.init({ config });

    CMS.registerLocale('nl', locales.nl);
</script>

<div/>